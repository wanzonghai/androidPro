<!-- res/layout/activity_add_student.xml -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/asdasgr">

    <EditText
        android:id="@+id/editTextStudentName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Student Name"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="8dp"
        android:inputType="textPersonName"/>

    <EditText
        android:id="@+id/editTextRollNumber"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Roll Number"
        android:layout_below="@id/editTextStudentName"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="8dp"
        android:inputType="text"/>

    <!-- 使用 Spinner 来代替 EditText，提供男女两个选项 -->
    <Spinner
        android:id="@+id/spinnerStudentGenderType"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Select Gender Type"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="8dp"
        android:layout_below="@id/editTextRollNumber"/> <!-- 放置在之前的 EditText 下方 -->

    <EditText
        android:id="@+id/editTextContactNumber"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Contact Number"
        android:layout_below="@id/spinnerStudentGenderType"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="8dp"
        android:inputType="text"/>
    <Button
        android:id="@+id/buttonSaveStudent"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:layout_below="@id/editTextContactNumber"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="Save"
        android:textSize="28dp"
        android:background="@drawable/gasa"/>

</RelativeLayout>
